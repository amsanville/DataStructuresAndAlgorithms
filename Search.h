#ifndef SEARCH_H
#define SEARCH_H
/*
Binary Search

*/
template<typename T>
int binarySearch(T* arr, int size, T item) {
	// Initialize starting indices
	int indexLower = 0;
	int indexUpper = size - 1;
	
	// Loop until either it's found or no more things need to be searched
	bool done = false;
	while (!done) {
		// TODO
		done = true;
	}
	return 0;
}

#endif